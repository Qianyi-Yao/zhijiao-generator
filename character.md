# 人物资料说明

160名人物的可运行资料统一存放在 `characters.js`。

每条人物资料包含：网页内部英文 `id`、游戏数字编号 `cardId`、姓名、朝代、稀有度、定位、头像路径与立绘路径。新增人物时，请同时将头像放入 `avatars/`、立绘放入 `portraits/`，再在 `characters.js` 的数组末尾增加对应资料。

### 备注

朝代有：先秦、秦、西楚、汉、三国、晋、南北朝、隋、唐、五代十国、宋、辽、元、明、清、不详、梦

定位有：输出、防御、辅助、控制、治疗

稀有度有：天、地、玄、黄
