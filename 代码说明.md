# 知交圈生成器：160人物版代码说明

此版本在更改排版版本的基础上接入160名人物，没有改动原有视觉排版和功能逻辑。请保持所有文件与素材文件夹的相对位置不变。

- `index.html`：只负责页面结构和外部文件调用。
- `style.css`：负责颜色、字号、间距、布局、立绘位置、评论样式及响应式显示。
- `characters.js`：负责160名人物的数据、头像路径和立绘路径。
- `app.js`：负责工程状态、实时预览、评论操作、保存导入及 PNG 导出。

最常用的修改入口：

- 调整体颜色：`style.css` 的“01. 字体与全局变量”。
- 调整编辑器外观：`style.css` 的“02～08”。
- 调整最终 PNG：`style.css` 的“09～11”。
- 增加人物：`characters.js` 的“01. 人物资料”。
- 修改默认内容：`app.js` 的“02. 默认工程与空白工程”。
- 修改按钮或功能：`app.js` 的“09. 控件事件绑定”。

人物字段中，`id` 是网页内部使用的英文唯一标识，`cardId` 是游戏数字编号。旧工程文件仍通过英文 `id` 识别人物。

本地开发与 PNG 导出请通过 Live Server 打开 `index.html`，不要直接使用 `file://` 地址。
